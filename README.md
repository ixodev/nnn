# NNN (Native Neural Networks)

A collection of very simple perceptrons made from scratch, with numpy as any external library.
To run, install the requirements with pip and then train the model you want